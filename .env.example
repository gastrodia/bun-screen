# Upstash Redis 的 TCP 连接串（用于 ioredis 的 pub/sub）
# 在 Upstash 控制台 → 你的数据库 → "Connect" → 选 ioredis / TCP，复制 rediss:// 开头的串
# 注意：不要用 https:// 的 REST URL，pub/sub 的 SUBSCRIBE 需要 TCP 连接
REDIS_URL=rediss://default:<password>@<your-db>.upstash.io:6379
